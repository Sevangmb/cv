import { createMuiTheme }  from '@material-ui/core/styles'

const theme = createMuiTheme({

  palette: {
    background: {
      default: "#f4f6ff"
    },
    primary: {
      main: '#4f8a8b',
    },
    secondary: {
      main: '#ffcb74',
    },
  },
});

export default theme